export default function OnePieceHeader({ title }) {
    return (
        <header className="site-header">
            <div className="inner">
                <h1>☠️ {title} 멤버 소개</h1>
            </div>
        </header>
    );
}